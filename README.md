# Be_Try — Vite + JS

Top-down arena shooter prototype for browser (Canvas).

## Scripts

```bash
npm install
npm run dev
```

## GitHub Pages

Repo name is assumed to be `Be_Try`, and `vite.config.js` is configured with:

```js
base: "/Be_Try/";
```

If the repo name is different, update `base` accordingly.

## Highscore

Stored in localStorage under key:

- `boxhead2_highscore`
- `boxhead2_bestWave`
- `boxhead2_bestTime`
